const { API_PATH } = require('./api-path.enum');
const { MESSAGES } = require('./messages.enum');
const { ERRORS } = require('./errors.enum');
const { STATUSES } = require('./statuses.enum');

module.exports = { API_PATH, MESSAGES, ERRORS, STATUSES };
