const ERRORS = {
  TO_MANY_REQUESTS: 'Too many requests, please try again later.',
  INDEX_NOT_INTEGER: 'Index must be a non-negative integer!',
  INVALID_JSON: 'Not valid JSON',
  TEST_ERROR: 'Test Error',
};

module.exports = { ERRORS };
